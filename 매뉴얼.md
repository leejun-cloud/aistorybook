# 매뉴얼

## 한 줄 소개
아이디어 → 스토리 → 캐릭터·스타일 → 그림·조판 → 조절·인쇄 순서로 인쇄 가능한 동화책 PDF(본문 2종 + 랩 표지)를 만드는 AI 동화책 제작 스튜디오 웹앱. 파트 1~4 전부 브라우저 UI에서 실행 가능.

## 설치

```bash
pnpm install                        # playwright·sharp 포함
npx playwright install chromium     # PDF 렌더용 브라우저 (최초 1회)
```

## 실행

```bash
pnpm dev      # 로컬 개발 서버, http://localhost:3000
pnpm build    # 프로덕션 빌드
pnpm start    # 빌드 후 프로덕션 서버 실행
```

## 배포 (Vercel)

- 저장소는 **이중 모드**: `BLOB_READ_WRITE_TOKEN`이 있으면 Vercel Blob(클라우드), 없으면
  로컬 파일시스템. 로컬 실습은 아무 설정 없이 파일시스템으로 동작한다.
- Vercel 배포 시 필요한 것: 프로젝트에 **Blob 스토어 연결**(`vercel blob create-store <이름> --access private`)
  + 환경변수 `GEMINI_API_KEY`(Production).
- PDF 렌더는 서버리스에서 `playwright-core + @sparticuz/chromium`을 쓴다 —
  `next.config.mjs`의 `outputFileTracingIncludes`가 chromium 바이너리(bin/)를 함수에 포함시킨다 (수정 주의).
- 배포: `vercel deploy --prod`. 주의: 공개 배포하면 모든 방문자가 서버의 GEMINI 키로 생성한다 —
  실습 후 키 사용량 확인·회전 권장.

## 필수 환경변수 (.env.local)

- `GEMINI_API_KEY` — 텍스트/이미지 생성 (Gemini). 필수
- `GEMINI_IMAGE_MODEL` (기본 gemini-2.5-flash-image), `GEMINI_TEXT_MODEL` (기본 gemini-2.5-flash) — 선택
- `GEMINI_STORY_MODEL` — 스토리 초안·게이트 전용 모델 승급 (선택, pro 계열 권장)
- `STORY_DRAFT_CANDIDATES` — best-of-N 초안 수 (기본 2, 1~4)
- `NEXT_PUBLIC_TOSS_CLIENT_KEY` / `TOSS_SECRET_KEY` — 이용권 결제(토스페이먼츠 단건).
  없으면 `/api/credits/order`가 503으로 막고 결제 버튼이 동작하지 않는다. 제작 단계는
  키 없이도 전부 쓸 수 있고, 인쇄용 파일 다운로드만 잠긴다.

## 이용권 · 잠금 (운영)

- 과금 단위는 **작업 프로젝트 1건**. 요금은 `lib/pricing.ts`가 정본:
  단건 ₩3,900 / 3건 ₩9,900(1건당 3,300) / 10건 ₩29,900(1건당 2,990).
  금액을 바꾸려면 이 파일만 고치면 된다 — 서버가 주문 금액의 정본이라 클라이언트가
  보낸 금액은 승인에 쓰이지 않는다.
- 잔액·주문·해제된 프로젝트는 스토리지의 `account/entitlements.json` 한 파일에 쌓인다
  (로컬은 파일시스템, 배포는 Blob). 로그인이 없어 계정은 하나 — 인증을 붙이면
  `lib/credits.ts`의 `accountFile()`에 사용자 id만 끼우면 된다.
- 잠그는 것: 인쇄용 본문 PDF, 인쇄용 랩 표지 PDF, 출판 패키지 ZIP.
  잠그지 않는 것: 열람용 미리보기 PDF (결제 전에 결과물을 볼 수 있어야 한다).
- 한 번 해제한 프로젝트(`publish.unlockedAt`)는 추가 결제 없이 계속 받을 수 있다.
- 잔액 확인: `GET /api/credits`. 수동 지급이 필요하면 `account/entitlements.json`의
  `credits`를 직접 올린다.

## 페이지 구성

- `/` 랜딩 — 히어로 삽화는 `public/landing/*.jpg` (codex `image_gen`으로 생성, 재생성 시
  글자 없는 프롬프트 유지)
- `/samples` 화풍 도감 · `/guide` 제작 과정 · `/pricing` 이용권
- `/dashboard` 작업실 (프로젝트 목록) — 예전에 `/`였다. 북마크가 랜딩으로 간다.
- `/story` `/character` `/layout` `/cover` `/publish` 제작 4단계
- `/payment/success` 결제 승인 착지점 — 여기서 서버 승인을 호출해야 실제로 적립된다.
  이 페이지를 건너뛰면 결제는 되고 이용권은 안 들어간다.

## 브라우저 사용 흐름 (대시보드에서 "새 동화책" 생성 후)

0. **대시보드** — "새 책 만들기"에서 **스타일 레퍼런스**를 고르면 저장해 둔 책의
   화풍(앵커 이미지)·분위기·연령·분량·조판이 그대로 적용된 채 시작한다 — 스토리와 인물만
   새로 만들면 같은 스타일의 책이 나온다. 반대로 완성한 책은 카드의 **"레퍼런스로 저장"**
   버튼으로 참조자료가 된다 (조건: 확정 그림 1장 이상).
1. **파트 1 `/story`** — 두 가지 시작 방법:
   - **브레인스토밍**: "💬 AI 작가도우미와 브레인스토밍" — 등장인물 → 배경 → 사건 → 느낌 → 연령
     순서로 대화하며 기획. "✨ 정리해서 스토리 만들기"를 누르면 기획안 정리(아이디어·분위기 자동
     입력 + 등장인물을 파트 2에 시드) → 곧바로 장면 대본 생성까지 이어진다.
   - **직접 입력**: 아이디어·연령·분량·느낌 입력 + 패턴 선택(비우면 AI 자동) → "스토리 생성".
   분량은 프리셋(8/12/16/20/24장면) 또는 **직접 입력(4~40)**.
   초안 2개를 비교 채점해 좋은 쪽을 채택한다. → "게이트 실행" (5종 채점 + 자동 수리 최대 2회).
   **게이트 통과(5/5) 전에는 승인 버튼이 잠긴다.** 장면 카드에서 직접 수정 가능(원문 보존),
   우측에서 장면별 AI 다시 쓰기.
2. **파트 2 `/character`** — 스타일 선택 3경로: ① 기본 라이브러리 6종 ② **출판 사례 스타일 6종**
   (출판 그림책 미학 계열을 스타일 특성으로 서술한 프리셋 — 화풍+분위기 제안+글 상자 기본값까지 복제)
   ③ 참고 그림 업로드(저작권 동의 필수, 1~3장 — 출판본 사진을 직접 참조할 때).
   "새 캐릭터 만들기"에 이름·설명 입력 → 후보 4장 생성 → 1장 선택 → "공식 레퍼런스로 확정"
   (이미지에서 텍스트 DNA 자동 추출). 부분 수정("목도리를 빨간색으로")으로 후보 추가 가능.
   **확정 캐릭터 1명 이상이어야 승인 가능.**
3. **파트 3 `/layout`** — "전체 장면 일괄 생성" 하나로 끝: 장면마다 후보 2장 생성 → DNA 일관성
   vision 채점 → 전원 80점 미만이면 위반 사유를 넣어 자동 재생성(최대 2회) → 최고점 후보 자동
   확정 + 템플릿 자동 추천. 페이지별 수동 재생성·후보 교체·확대이동·글꼴 편집도 가능.
   그림 위 글은 **"글 상자" 선택**으로 반투명 상자 / 상자 없음(종이색 글로우) 중 고른다
   (페이지별 설정, 프리셋/레퍼런스가 기본값 지정 가능). **모든 페이지에 그림이 있어야 승인 가능.**
4. **파트 4 `/publish`** — 표지 3방향 생성 → 선택 → 책등 계산 + 랩 표지 PDF → 사전검사 →
   본문 PDF 렌더(열람용 + 인쇄용). **인쇄용 렌더 시 300dpi 업스케일이 자동 실행**되어
   `assets/*@print.jpg` 변형본을 만들고 그것으로 렌더한다.
   - **검수 점수** — 사전검사 결과가 0~100점으로 나온다 (FAIL −20, WARN −5).
   - **"자동 수정 가능한 항목 처리"** — 글 넘침은 Typst 실측을 반복하며 글자 크기를
     11px까지 줄여 해소하고, 저해상도는 인쇄 변형본을 만든다. 조판이 바뀌므로 프로젝트를
     다시 읽어 온다. 못 고친 항목은 따로 안내된다 (페이지 누락·안전영역·폰트는 손대지 않는다).
   - **최종 출판 패키지** — 저자·출판사·ISBN·책 소개·유통 플랫폼을 입력하면 입력됨/누락됨
     배지가 뜬다. ISBN은 입력 즉시 체크디지트를 검증하고, 유효하면 **뒷표지에 EAN-13
     바코드가 인쇄된다**(`lib/cover/barcode.ts`, 규격 폭 37.29mm 벡터).
     "인쇄소 제출 패키지 다운로드"는 인쇄용 본문·표지 PDF + 열람용 PDF + 서지정보.txt +
     검수리포트.txt 를 ZIP 한 덩어리로 준다.
   - **잠금 해제** — 잔액이 있으면 이용권 1건을 바로 차감하고, 없으면 결제창으로 간다.
5. **표지 편집 `/cover`** — 조절·인쇄의 "표지 상세 편집" 링크. 랩 표지(뒤+책등+앞) 실시간
   미리보기에서 제목·작가명·**책등 글**·뒷표지 문구를 편집하고, 제목/작가명의 **세로 위치(%)·
   크기(pt)·색**을 슬라이더로 조절. 규격(프로파일·제본·종이·페이지 수)을 바꾸면 책등 폭이 즉시
   재계산된다. "편집 저장" 후 렌더하면 PDF에 그대로 반영.

## 데이터 저장 위치

- 프로젝트 데이터: `projects/<id>/project.json`, 이미지: `projects/<id>/assets/`, PDF: `projects/<id>/output/`
- 스타일 레퍼런스(참조자료): `references/<id>/reference.json` + 앵커 이미지
- 인쇄 변형본: `projects/<id>/assets/<이름>@print.jpg` (2552px = 216mm 풀블리드 300dpi)
- git에는 커밋되지 않음 (`.gitignore`에 `projects/` 포함)

## 테스트

```bash
pnpm test:spine   # 책등 계산 단위 테스트 (print-profiles.md §1 검산)
pnpm test:e2e     # 골든 런: 아이디어 → 스토리·게이트 → 캐릭터·DNA → 장면 8개(일관성 교정)
                  #   → 표지·책등 → 업스케일 → 사전검사 → PDF 3종. 실 API 이미지 20~30회, 수 분 소요
npx tsx tests/scene/run-sample.ts        # 파트 3 부분 통합 (구버전)
npx tsx tests/publish/run-integration.ts # 파트 4 부분 통합 (구버전)
```

골든 런 결과는 `tests/e2e/run-record.json`에 기록된다 (게이트·장면별 DNA 점수·사전검사·PDF 크기).

## 운영 팁

- `?project=` 쿼리 없이 파트 페이지 접속 시 저장되지 않는 목업 미리보기로 동작
- 출판 규격의 정본은 `research/print-profiles.md` — 종이 두께·판형 등 계수는 `lib/cover/profiles.ts` 데이터만 갱신
- 조판 템플릿의 정본은 `lib/render/templates.ts` (UI 미리보기·PDF 렌더가 같은 정의 사용)
- 스토리 품질을 더 올리려면: `GEMINI_STORY_MODEL`을 pro 계열로, `STORY_DRAFT_CANDIDATES=3`
- 무선제본 표지 "안쪽면"을 출력하게 되면 책등 접착부 ±5mm는 백지 필수 (`lib/cover/wrap.ts` 주석 참조)
- 장면 재생성 라운드 에셋은 `-r2`, `-r3` 접미사로 구분 저장 — 원본 후보를 덮어쓰지 않음
