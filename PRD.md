# AI 동화책 제작 스튜디오 PRD v2.0

**프로젝트:** aistorybook
**문서 버전:** 2.0 (v1.0을 실전 검토 결과로 재설계)
**작성일:** 2026년 8월 31일
**전신:** `AI 동화책 제작 플랫폼 PRD v1.0` (Hope Story Studio)
**대상:** 학생, 부모, 교사, 작가

---

## 0. v1.0에서 무엇이 달라졌나

v1.0을 기존 자산 4개(bookforge, lecture-video timeline, TextbookBuilder, PublishReadyAI)와
최신 이미지 생성 모델의 실제 능력에 비추어 검토한 결과를 반영했다.

| 항목 | v1.0 | v2.0 | 이유 |
|---|---|---|---|
| 스토리 생성 | 17개 요소 추출 + 4범주 부족판별 + 단계별 질문 트리 | **메타프롬프트 + 플롯 패턴 라이브러리** | 과도한 지침은 AI의 창조성을 방해. 현행 모델은 좋은 메타프롬프트만으로 구조가 잡힌 이야기를 씀 |
| 캐릭터 일관성 | Character DNA + 10종 설정표(정면·45도·옆·뒤·표정…) | **확정 레퍼런스 1장 + 장면별 레퍼런스 조건부 생성** | 최신 모델(Gemini 이미지 생성 등)은 레퍼런스 1장으로 장면·각도가 바뀌어도 일관성 유지. 설정표는 선택 기능으로 격하 |
| 스타일 지정 | Style Bible JSON 수동 스키마 | **참고 그림 업로드 → 스타일 서술 자동 추출 + 레퍼런스 이미지 동시 전달** | 서술어 변환(v1.0 방식)만으로는 한계. 이미지 자체를 컨디셔닝 입력으로 전달 |
| 조판 렌더링 | 기술스택만 제시(Paged.js 등) | **bookforge HTML 트랙 패턴 이식** (theme.html + theme.css + Playwright, 재단선·안전영역·2-pass 쪽번호 검증 완료) | 이미 동작 검증된 자산. Vivliostyle 등 새 엔진 불필요 — 그림책은 페이지 구조가 단순해 무거운 조판 기능이 필요 없음 |
| 편집 UI | Fabric.js 후속 검토 | **슬롯 편집 우선, 자유 편집은 Konva.js로 후속** (gzip ~50kB, react-konva) | Fabric은 무겁고(gzip ~90kB) MVP에 불필요 |
| AI 수정 | 화면 버튼 중심 | **MCP 도구 노출 — 대화로 "이 그림만 다시" 수정** | lecture-video timeline의 API 라우트 패턴 위에 MCP 서버를 얹는 검증된 경로 |
| 워크플로 | 8단계 승인 | **4파트 × 파트별 승인 1회** | 승인 지점 과다는 흐름을 끊음. TextbookBuilder의 "기준본 승인 → 이후 자동" 패턴 차용 |

v1.0의 핵심 철학은 그대로 유지한다:
**사용자의 이야기를 잃지 않는다 · 글과 그림을 분리한다 · 전체 재생성 대신 부분 수정 · 참고와 복제를 구분한다.**

---

## 1. 제품 정의

사용자가 이야기 아이디어를 넣으면 —
**① 스토리 → ② 캐릭터·스타일 → ③ 그림 생성·조판 → ④ 조절·인쇄**
네 파트를 거쳐 인쇄 가능한 동화책 PDF를 완성하는 **웹앱**이다.

### 제품이 지켜야 할 세 가지 품질 서약

1. **스토리에서 AI 티가 나면 안 된다.** 재미있고, 교훈이 있고, 감동이 있어야 한다.
2. **그림은 책 전체에서 같은 인물, 같은 화풍이어야 한다.**
3. **출력물은 화면과 같아야 한다.** (미리보기 = 인쇄 PDF)

---

## 2. 사용자에게 보이는 흐름

```text
1. 스토리          아이디어 입력 → AI 초안 → 다듬기 → 승인
2. 캐릭터·스타일    참고 그림 업로드/스타일 선택 → 캐릭터 후보 → 확정
3. 그림·조판        장면별 그림 생성 → 자동 조판 → 펼침면 확인
4. 조절·인쇄        슬롯 조정 → 표지 → 사전검사 → PDF
```

각 파트 끝에 승인 1회. 승인 전까지는 그 파트 안에서 자유롭게 수정/재생성.

---

# 파트 1 — 스토리

## 1.1 설계 원칙: 지시하지 말고 방향만 준다

v1.0의 요소 추출·질문 트리 파이프라인을 폐기하고, 두 레이어로 단순화한다.

### 레이어 A. 플롯 패턴 라이브러리 (구조를 빌린다)

베스트셀러·고전에서 **문장이 아니라 구조 패턴**만 추출해 라이브러리로 보관한다.

```text
patterns/
├── quest.md            # 결핍 → 여정 → 시련 → 조력 → 귀환 (오디세이아형)
├── transformation.md   # 약점 → 사건 → 선택 → 변화 (미운 오리 새끼형)
├── friendship.md       # 만남 → 오해 → 위기 → 화해 (샬롯의 거미줄형)
├── courage.md          # 두려움 → 회피 → 직면 계기 → 극복 (괴물들이 사는 나라형)
├── kindness-ripple.md  # 작은 선행 → 뜻밖의 연결 → 되돌아옴 (행복한 왕자형)
└── ...
```

각 패턴 파일은 다음만 담는다 (원작 문장·캐릭터·고유 설정 절대 포함 금지 — v1.0 §5.7 계승):
- 감정 곡선 (어디서 웃기고 어디서 뭉클해야 하는가)
- 장면 비트 (8/12/16 장면 기준 배치)
- 이 패턴이 잘 먹히는 연령·주제
- 이 패턴에서 흔한 실패 (교훈 직접 설명, 갑작스러운 해결 등)

### 레이어 B. 메타프롬프트 (AI 티 제거)

시스템 프롬프트는 "무엇을 쓰라"가 아니라 "무엇을 하지 말라 + 어떤 기준을 통과하라"로 구성한다.

**금지 목록 (AI 티의 실체):**
- 교훈을 마지막에 문장으로 설명하는 것 ("~하다는 것을 깨달았어요")
- 모든 문장이 같은 길이·같은 리듬
- 감정을 형용사로 서술 ("슬펐어요") — 행동으로 보여라 ("문 뒤에서 한참 나오지 않았어요")
- 갈등이 저절로 풀리는 결말
- 부사 남발, "그러던 어느 날" 류의 상투적 전환구
- 캐릭터가 나이에 안 맞게 어른스러운 대사

**통과 기준 (스토리 품질 게이트):**
- 주인공이 절정에서 스스로 선택하는가
- 소리 내어 읽었을 때 리듬이 사는가 (그림책은 낭독물이다)
- 교훈이 행동과 결과로 드러나는가
- 한 장면 안에서 실제 사건이 진행되는가 (정지된 묘사 페이지 금지)
- 다시 읽고 싶은 마지막 문장인가

### 1.2 생성 흐름

```text
사용자: 아이디어 입력 (한 문장이어도 됨)
  + 대상 연령 / 분량(8·12·16장면) / 원하는 느낌 선택
→ AI: 어울리는 플롯 패턴 1~2개 자동 선택 (사용자가 바꿀 수 있음)
→ AI: 초안 생성 (메타프롬프트 + 패턴)
→ 자동 품질 게이트 실행 → 미달 항목은 AI가 스스로 1회 수정
→ 사용자: 장면별로 읽고 문장 직접 수정 or "이 장면 다시" 요청
→ 승인
```

원문 보존 원칙(v1.0 §5.3): 사용자가 직접 쓴 문장과 AI 문장은 데이터에서 구분 저장.
사용자가 수정한 문장은 이후 재생성에서 AI가 덮어쓰지 않는다.

### 1.3 장면 데이터 (v1.0 §8.3.2 유지, 간소화)

```json
{
  "sceneNumber": 8,
  "beat": "결정적 선택",
  "text": "토끼는 겁이 났지만 친구를 두고 갈 수는 없었어요.",
  "textSource": "user | ai",
  "characters": ["rabbit"],
  "location": "폭풍이 부는 숲길",
  "emotion": "두려움과 결심",
  "visualFocus": "돌아선 발걸음",
  "preferredTextArea": "upper-left"
}
```

---

# 파트 2 — 캐릭터와 전체 스타일 디자인

## 2.1 스타일: 참고 그림에서 시작한다

```text
경로 A. 참고 그림 업로드 (우리 프로젝트의 기본 경로)
  사용자가 좋아하는 그림책 사진 1~3장 업로드
  → AI가 스타일 서술 추출 (재료·선·팔레트·질감·배경 밀도)
  → 이 서술 + 업로드 이미지 자체를 이후 모든 생성의 컨디셔닝 입력으로 사용

경로 B. 기본 스타일 라이브러리에서 선택
  수채화 / 색연필 / 과슈 / 콜라주 / 3D 소프트 / 민화풍 등
  각 스타일은 대표 이미지로 보여준다 (이름만 나열 금지 — v1.0 §8.5.1 유지)
```

저작권 원칙(v1.0 §5.7 전문 계승): 원작 캐릭터·문장·로고·고유 구도는 생성 입력에서 제외.
스타일(색감·선·질감·비율)만 참고한다. 업로드 시 확인 문구 동의 필수.

## 2.2 캐릭터: 레퍼런스 1장이 전부다

```text
인물 한두 문장 설명
→ 후보 4장 생성 (실루엣·체형·머리 의도적 차별화 — v1.0 §8.6.3 유지)
→ 1장 선택 (필요시 "머리만 바꿔줘" 부분 수정)
→ 확정 = 이 이미지가 공식 레퍼런스가 된다
```

- **설정표(다각도 시트)는 만들지 않는다.** 최신 모델은 레퍼런스 1장으로 충분하다.
  (원하는 사용자를 위해 "설정표 뽑기"를 선택 메뉴로만 남긴다)
- 확정 캐릭터마다 저장: 레퍼런스 이미지 + 짧은 텍스트 DNA(고정 요소: 머리·의상·소품 / 금지 요소).
  텍스트 DNA는 생성 프롬프트에 함께 들어가 레퍼런스를 보강한다.
- 등장인물 2인 이상이면 함께 보기 + 유사 경고 (v1.0 §8.6.8 유지).
- 캐릭터는 프로젝트 간 재사용 가능 (시리즈).

## 2.3 착수 전 필수 검증 (이 프로젝트의 1번 리스크)

구현에 앞서 **Gemini 이미지 생성 API로 다음을 실측한다:**

```text
같은 레퍼런스 1장 + 서로 다른 장면 설명 5개
→ 5장 연속 생성
→ 얼굴·의상·화풍 일관성 육안 + 자동 평가
```

이 테스트가 통과해야 파트 3 이후 설계가 유효하다.
미달이면 대안(레퍼런스 2~3장 번들, ComfyUI+IP-Adapter 자체 호스팅)을 검토한다.

---

# 파트 3 — 그림 생성 및 글 조판

## 3.1 그림 생성

장면별 생성 입력:

```text
캐릭터 레퍼런스 이미지(들) + 텍스트 DNA
+ 스타일 참고 이미지 + 스타일 서술
+ 장면 데이터 (장소·감정·행동·visualFocus)
+ 조판 예약 영역 (글이 들어갈 곳은 배경을 단순하게)
+ 금지: 이미지 안에 글자·워터마크 (v1.0 §8.7.3 유지)
```

- 페이지당 후보 2장 저해상도 → 선택 후 업스케일 (비용 원칙, v1.0 §14 유지)
- 실패한 페이지만 재시도. 한 페이지 실패가 책 전체를 막지 않는다.
- 자동 검사: 캐릭터 고정 요소 유지 / 이미지 내 글자 / 해상도 (v1.0 §8.7.6 축약판)

## 3.2 조판: bookforge HTML 트랙 이식

이미 검증된 자산을 가져온다. 새로 만들지 않는다.

```text
가져오는 것 (bookforge magazine/insight 스타일에서):
- theme.html + theme.css 패턴 (string.Template, @page 규칙, 풀블리드 섹션)
- Playwright/Chromium 인쇄 PDF 렌더링
- 재단선(bleed)·안전영역 처리
- 2-pass 쪽번호 주입
- decorate.py 스탬핑 패턴 (필요시)

새로 만드는 것:
- 그림책용 조판 템플릿 10종 (v1.0 §8.8.3의 L01~L10 그대로 채택)
- 페이지 JSON → HTML 렌더러 (글·그림 분리 저장, v1.0 §8.8.2 스키마 채택)
```

- 글은 절대 이미지 안에 넣지 않는다. 본문·제목은 HTML/CSS/SVG.
- AI가 장면당 템플릿 자동 추천 (글자 수·인물 위치·장면 중요도 기준, v1.0 §8.8.6).
- 한 권에서 핵심 템플릿 3~5개 중심, 같은 템플릿 연속 반복 회피.

---

# 파트 4 — 조절 및 최종 인쇄

## 4.1 슬롯 편집 (MVP)

v1.0 §8.8.4 채택: 정해진 슬롯 안에서만 조정한다.

- 그림 슬롯: 확대·축소 / 이동 / 자르기 / 교체 / 다시 생성
- 글 슬롯: 문장 수정 / 크기 / 행간 / 정렬 / 색
- 자동 제한: 안전영역 밖 배치 금지, 글 넘침 경고, 얼굴 위 텍스트 경고

자유 편집(회전·레이어·도형)은 후속 단계에서 **Konva.js(react-konva)** 로 추가한다.
Fabric.js는 쓰지 않는다 (무겁고 슬롯 모델과 충돌).

## 4.2 MCP 편집 도우미

lecture-video timeline의 API 라우트 패턴 위에 MCP 서버를 얹어,
편집 화면에서 대화로 부분 수정을 지시할 수 있게 한다.

```text
노출 도구 (예):
- regenerate_scene_image(sceneId, note)   "7페이지 토끼 표정을 웃는 얼굴로"
- edit_scene_text(sceneId, instruction)   "이 문장 더 짧게"
- change_layout(sceneId, templateId)
- fix_character(sceneId, element)         "가방이 빠졌어, 다시"
```

## 4.3 표지

v1.0 §8.9 축약 채택:
- 표지 그림과 제목 글자 분리 (그림=AI, 제목·작가명=HTML/SVG)
- 서로 다른 3방향 제안 (캐릭터 중심 / 장면 / 상징)
- 썸네일 가독성 검사
- **출판 표지는 앞표지+책등+뒷표지 랩 PDF 필수** (MVP 범위로 승격 — 사용자 확정)

### 책등 계산 — 페이지 수만으로 계산하지 않는다 (사용자 확정)

`제본 방식 + 내지 종이 + 페이지 수` 3요소를 입력받아 계산한다. 정본 규격: **research/print-profiles.md**
- 중철(16~32p): 책등 없음 / 무선: (페이지÷2)×종이두께+풀 1mm, 0.5mm 반올림 / 양장: +5mm / 보드북: 별도 계산
- 책등 폭 < 5mm면 책등 텍스트 자동 생략 + 사용자 알림
- 무선제본 표지 안쪽 책등부±3~5mm 백지(접착) 규칙

### 출판 프로파일 — 교보 POD · 부크크 (사용자 확정 유통 경로)

판형·여백·표지 재질·제본 제약은 research/print-profiles.md의 **명명된 프로파일**(부크크1~4, 교보1~3)을 정본으로 사용. 조판의 페이지 크기 결정과 사전검사가 같은 프로파일을 참조한다.

## 4.4 출력과 사전검사

```text
MVP 출력: 열람용 PDF / 인쇄용 본문 PDF(재단선 포함) / 인쇄용 표지 PDF / 웹북 / 페이지 PNG
사전검사: 페이지 누락·순서 / 글 넘침 / 안전영역 침범 / 이미지 실해상도 / 폰트 임베드
```

bookforge의 QC 게이트 사상을 잇되, 그림책에 맞는 항목만 남긴다
(텍스트 밀도 게이트류는 그림책에 부적합 — 제외).

---

# 5. 기술 구조

```text
Frontend   Next.js + React + TypeScript + Tailwind
           슬롯 편집: CSS Grid + 제한된 컨트롤 (후속: react-konva)
Backend    Next.js API Routes (lecture-video timeline 패턴)
           작업 큐: 페이지별 이미지 생성 잡 (BullMQ 또는 단순 잡 테이블)
AI         텍스트: Gemini (스토리·스타일 분석·조판 추천)
           이미지: Gemini 이미지 생성 (레퍼런스 조건부) — §2.3 검증 후 확정
           어댑터 구조로 공급자 교체 가능 (v1.0 §10 유지)
조판/출력   HTML + CSS + Playwright (bookforge 패턴 이식)
저장       MVP: 파일시스템 + 프로젝트 JSON / 후속: PostgreSQL
MCP        편집 도구 노출 (§4.2)
```

기존 자산 재사용 지도:

| 자산 | 가져올 것 |
|---|---|
| bookforge | HTML 조판 트랙, Playwright PDF, 재단선/안전영역, QC 사상 |
| lecture-video timeline | Next.js 구조, API 라우트 패턴, 좌측검색+중앙미리보기+우측Inspector 3패널 UI, Gemini 연동 코드 |
| TextbookBuilder | 승인 워크플로 사상, kids-bright 등 어린이 디자인 토큰 감각 |
| PublishReadyAI | 원문/AI 구분 저장 패턴, 코칭형 UX |

---

# 6. MVP 범위

## 포함

- 파트 1: 아이디어 입력 → 패턴 선택 → 초안 → 품질 게이트 → 장면별 수정 → 승인
- 파트 2: 참고 그림 업로드 분석 + 기본 스타일 6종 / 캐릭터 후보 4 → 확정 레퍼런스
- 파트 3: 장면별 그림 생성(후보 2) + 조판 템플릿 10종 자동 배치
- 파트 4: 슬롯 편집 + 표지(앞+책등+뒤 랩) + 책등 계산기(제본·종이·페이지) + 열람/인쇄 PDF + 사전검사
- 프로젝트 저장/불러오기 (JSON)

## 제외 (후속)

- Konva 자유 편집 / MCP 편집 도우미(2차)
- 회원·결제 / 멀티유저 / EPUB / 오디오북 / 인쇄 주문 / ISBN
- v1.0의 설정표 10종, 질문 트리, Style Bible 수동 편집

---

# 7. 개발 순서

```text
0단계  [검증] Gemini 레퍼런스 조건부 생성 일관성 실측 (§2.3) — 통과해야 진행
1단계  스토리: 패턴 라이브러리 5종 + 메타프롬프트 + 품질 게이트 + 장면 편집 UI
2단계  캐릭터·스타일: 업로드 분석, 후보 생성, 확정 레퍼런스 저장
3단계  그림·조판: 장면별 생성 잡 + bookforge 패턴 이식 + 템플릿 10종
4단계  조절·인쇄: 슬롯 편집 + 표지 + PDF + 사전검사
5단계  (후속) MCP 편집 도우미, Konva, 랩 표지, 계정
```

---

# 8. 성공 기준

- 완성된 책을 읽은 사람이 "AI가 쓴 것 같다"고 느끼지 않는다 (블라인드 테스트).
- 12장면 책에서 캐릭터 일관성 지적이 나오는 페이지가 2장 이하.
- 스토리 승인까지 사용자 수정 횟수가 장면당 평균 1회 이하.
- 첫 PDF 사전검사 통과율 80% 이상.
- 아이디어 입력부터 PDF까지 1시간 이내 (이미지 생성 대기 포함).
